# ===== 管理端 Web 构建变量 =====
# Vite 变量会在构建时写入静态文件，任何能看到管理端页面的人都可以读取；禁止填写密钥或 Token。
# 修改后必须重启 dev server；生产环境修改后必须重新构建管理端。

# 管理 API 根地址，必须包含 /api/admin/v1。
# 本地开发留空或删除本行时走 Vite 代理；远程部署应填写管理端浏览器可访问的 HTTPS 地址。
VITE_ADMIN_API_BASE=https://api.example.com/api/admin/v1
