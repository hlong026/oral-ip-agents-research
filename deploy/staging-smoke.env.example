# Private Staging smoke credentials. Copy outside the repository, chmod 0600,
# and provide only a dedicated Staging test account/code. Never reuse production credentials.
STAGING_SMOKE_ACTIVATION_CODE=REPLACE_WITH_STAGING_TEST_ACTIVATION_CODE
STAGING_SMOKE_DEVICE_FINGERPRINT=staging-smoke-device-v1
